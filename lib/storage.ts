// Shared chrome.storage keys. Kept in one place so popup.tsx (the writer)
// and the content scripts that read the setting can't drift out of sync.
export const DARK_MODE_STORAGE_KEY = "darkModeEnabled"
